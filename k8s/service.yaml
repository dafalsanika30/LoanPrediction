apiVersion: v1
kind: Service
metadata:
  name: loan-svc-2401034
  namespace: student-2401034
spec:
  selector:
    app: loan-app-2401034
  type: NodePort
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30234   # unique port (different for every student)
